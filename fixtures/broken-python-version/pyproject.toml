[project]
name = "broken-demo"
version = "0.1.0"
description = "A deterministic Yoink demo fixture: build fails because the Python requirement exceeds the generated base image."
requires-python = ">=3.13"
dependencies = ["flask"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
